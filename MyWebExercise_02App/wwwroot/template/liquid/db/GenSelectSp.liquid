USE [{{ table.Database_name }}]
GO

/****** Object:  StoredProcedure [dbo].[TP_SELECT_{{ table.Table_name }}]    Script Date: {{ "now" | date: "%Y/%m/%d %H:%M" }} ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

-- 查詢單筆資料 SP
CREATE PROCEDURE [dbo].[TP_SELECT_{{ table.Table_name }}] (
{%- assign param = "" -%}
{%- for column in table.columns -%}
{%- if column.Is_primary_key -%}
{%- if param == "" -%}
@{{ column.Column_name }} {{ column.Full_data_type }}
{%- assign param = @column.Column_name -%}
{%- else -%}
,@{{ column.Column_name }} {{ column.Full_data_type }}
{%- endif -%}
{%- endif -%}
{%- endfor -%}
)
AS
BEGIN
    SELECT * FROM {{ table.Schema_name }}.{{ table.Table_name }}
{%- assign param = "" -%}
{%- for column in table.columns -%}
{%- if column.Is_primary_key -%}
{%- if param == "" -%}
    WHERE {{ column.Column_name }} = @{{ column.Column_name }}
{%- assign param = @column.Column_name -%}
{%- else -%}
    AND {{ column.Column_name }} = @{{ column.Column_name }}
{%- endif -%}
{%- endif -%}
{%- endfor -%}
END